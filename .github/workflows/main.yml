# SPDX-FileCopyrightText: 2024-2026 Friedrich von Never <friedrich@fornever.me>
#
# SPDX-License-Identifier: MIT

# This file is auto-generated.
name: Main
on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
  schedule:
  - cron: 0 0 * * 6
  workflow_dispatch: 
jobs:
  verify-workflows:
    runs-on: ubuntu-24.04
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_NOLOGO: 1
      NUGET_PACKAGES: ${{ github.workspace }}/.github/nuget-packages
    steps:
    - name: Check out the sources
      uses: actions/checkout@v6
    - name: Set up .NET SDK
      uses: actions/setup-dotnet@v5
    - name: Cache NuGet packages
      uses: actions/cache@v5
      with:
        key: ${{ runner.os }}.nuget.${{ hashFiles('**/*.*proj', '**/*.props') }}
        path: ${{ env.NUGET_PACKAGES }}
    - run: dotnet fsi ./scripts/github-actions.fsx verify
  meganob-tests:
    strategy:
      matrix:
        image:
        - macos-15
        - ubuntu-24.04
        - ubuntu-24.04-arm
        - windows-11-arm
        - windows-2025
      fail-fast: false
    runs-on: ${{ matrix.image }}
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_NOLOGO: 1
      NUGET_PACKAGES: ${{ github.workspace }}/.github/nuget-packages
    steps:
    - name: Check out the sources
      uses: actions/checkout@v6
    - name: Set up .NET SDK
      uses: actions/setup-dotnet@v5
    - name: Cache NuGet packages
      uses: actions/cache@v5
      with:
        key: ${{ runner.os }}.nuget.${{ hashFiles('**/*.*proj', '**/*.props') }}
        path: ${{ env.NUGET_PACKAGES }}
    - name: Run tests
      shell: pwsh
      run: dotnet test
  build:
    strategy:
      matrix:
        image:
        - macos-15
        - ubuntu-24.04
        - ubuntu-24.04-arm
        - windows-11-arm
        - windows-2025
      fail-fast: false
    runs-on: ${{ matrix.image }}
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
      DOTNET_NOLOGO: 1
      MEGANOB_CACHE_BASE: ${{ github.workspace }}/.meganob
      NUGET_PACKAGES: ${{ github.workspace }}/.github/nuget-packages
    steps:
    - name: Check out the sources
      uses: actions/checkout@v6
    - name: Set up .NET SDK
      uses: actions/setup-dotnet@v5
    - name: Cache NuGet packages
      uses: actions/cache@v5
      with:
        key: ${{ runner.os }}.nuget.${{ hashFiles('**/*.*proj', '**/*.props') }}
        path: ${{ env.NUGET_PACKAGES }}
    - name: Install DOSBox-X
      shell: pwsh
      run: scripts/Install-DOSBox-X.ps1
    - name: Build system cache
      uses: actions/cache@v5
      with:
        key: ${{ runner.os }}.meganob.v1
        path: ${{ env.MEGANOB_CACHE_BASE }}
    - name: Build the game
      shell: pwsh
      run: dotnet run --project Build -- --verbose
    - if: ${{ matrix.image == 'ubuntu-24.04' }}
      name: Upload the game
      uses: actions/upload-artifact@v6
      with:
        path: out/*
